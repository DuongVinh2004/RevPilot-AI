# TASK-P01-004 — Implement Hidden Ground Truth and Scenario Injection Boundary

TYPE: MICRO-TASK
STATUS: PASS
RAIL: 12
INITIATIVE: REVPILOT
PHASE: PHASE-01
EPIC: PHASE-01-CANONICAL-DATA
FEATURE: HIDDEN-GROUND-TRUTH-SCENARIO
COMPLEXITY: S
REASONING_LOAD: LOW
READINESS_SCORE: 20/20
NEW_ARCHITECTURAL_DECISIONS_ALLOWED: 0
PARALLEL_SAFE: NO
PARALLEL_WITH: NONE
DEPENDS_ON: TASK-P01-003
UNLOCKS: TASK-P01-006

---

## Objective and rationale

OBJECTIVE: Implement the scenario injection logic for the Midwest truck-capacity causal incident (`INC-SYNTH-TRUCK-001`) and enforce the physical and logical air-gap boundary between hidden ground truth and runtime-visible canonical datasets in `packages/backend/src/revpilot/modules/benchmark/scenarios/`.

BUSINESS RATIONALE: Provide an objective, statistically sound ground-truth incident with known mechanistic causes, injected confounders, unaffected control populations, and true counterfactual estimands (`ATE = +0.0660`), while strictly preventing label leakage to the agent investigation runtime (`BR-001`, `FR-ML-004`, `INV-DATA-001`, `NFR-AI-002`, `AC-001`, `AC-006`).

ARCHITECTURAL OWNER: AI/ML Architecture + Security Architecture
ARCHITECTURAL CONTEXT: Conforms to `docs/03-requirements/SYNTHETIC-DATASET-SPEC.md` §5, §6, §7, `docs/20-evaluation/EVALUATION-FRAMEWORK.md` §3, and `docs/03-requirements/INVARIANT-REGISTRY.md` (`INV-DATA-001`, `INV-TEN-001`).

---

## Canonical specification references

- `docs/03-requirements/SYNTHETIC-DATASET-SPEC.md` §5, §6, §7
- `docs/20-evaluation/EVALUATION-FRAMEWORK.md` §3
- `docs/03-requirements/INVARIANT-REGISTRY.md` (`INV-DATA-001`, `INV-TEN-001`)
- `execution/MICRO-TASK-RAIL-SYSTEM.md` §rail-sequence

---

## Preconditions

1. `TASK-P01-003` (Deterministic Synthetic Generator) is completed.
2. Canonical data models and metric registry definitions are operational.
3. Air-gap access control specification in `SYNTHETIC-DATASET-SPEC.md` §7 is accepted.

---

## Change boundary

READ_SET:
- `docs/03-requirements/SYNTHETIC-DATASET-SPEC.md`
- `packages/backend/src/revpilot/modules/benchmark/generator.py`
- `packages/backend/src/revpilot/modules/canonical/models.py`

WRITE_SET:
- `packages/backend/src/revpilot/modules/benchmark/scenarios/__init__.py`
- `packages/backend/src/revpilot/modules/benchmark/scenarios/truck_capacity.py`
- `packages/backend/src/revpilot/modules/benchmark/ground_truth.py`
- `tests/benchmark/test_scenario_injection.py`
- `tests/eval/test_ground_truth_airgap.py`

CREATE:
- `packages/backend/src/revpilot/modules/benchmark/scenarios/__init__.py`
- `packages/backend/src/revpilot/modules/benchmark/scenarios/truck_capacity.py`
- `packages/backend/src/revpilot/modules/benchmark/ground_truth.py`
- `tests/benchmark/test_scenario_injection.py`
- `tests/eval/test_ground_truth_airgap.py`

MODIFY:
- NONE

DO_NOT_MODIFY:
- `packages/backend/src/revpilot/shared/*`
- `packages/backend/src/revpilot/modules/canonical/*`

EXPECTED CHANGE SET: Implement scenario injector for `INC-SYNTH-TRUCK-001` that modifies shipment dispatch times and cancellation states on Midwest cohort while keeping ground-truth labels isolated in an evaluation-only data container.

---

## Symbol-level contract

MUST EXPORT/DEFINE:
- `GroundTruthIncident(tenant_id: TenantId, incident_id: str, scenario_name: str, causal_start_time: UtcDateTime, causal_end_time: UtcDateTime, primary_root_cause: str, affected_facility_id: str, affected_carrier_id: str, true_ate_cancellation_rate_delta: float, true_delayed_shipment_count: int, true_revenue_at_risk_cents: int, competing_hypotheses: List[Dict[str, Any]])`
- `TruckCapacityScenarioInjector`:
  - `inject(dataset: GeneratedDatasetBundle, seed: int) -> Tuple[GeneratedDatasetBundle, GroundTruthIncident]`
- `EvaluationGroundTruthStore`:
  - `save_ground_truth(incident: GroundTruthIncident, target_path: str) -> None`
  - `load_ground_truth_for_eval(target_path: str, auth_token: EvaluatorAuthToken) -> Result[GroundTruthIncident, AccessDeniedError]`

MUST NOT:
- Embed `is_ground_truth` or `true_cause` inside runtime `OrderRecord`, `ShipmentRecord`, or `SupportTicketRecord`.
- Permit runtime code or non-evaluator principals to read ground-truth records.
- Mutate `ten_beta` control population during Midwest scenario injection.

---

## Input/output and validation

INPUT: Base generated dataset bundle and scenario seed.
OUTPUT: Modified bundle with realistic operational disruption symptoms + isolated `GroundTruthIncident`.
VALIDATION:
- Cancellation rate in `ten_alpha` Midwest cohort increases from baseline ~1.8% to 8.4% (+/- 0.5%).
- Unaffected facilities (`WH-WEST-01`) and control tenant (`ten_beta`) maintain baseline cancellation rates.
- Runtime records have 0 ground-truth labels or leakage markers.

---

## Error contract

| Code | Trigger | HTTP/status | Retryable | Client-safe message | Audit | Metric |
|---|---|---|---:|---|---:|---|
| `GROUND_TRUTH_ACCESS_DENIED`| Runtime principal requests truth | 403 | no | Access to evaluation truth is forbidden | yes | `ground_truth_security_violation_total` |
| `SCENARIO_INJECTION_FAILED` | Inconsistent cohort timestamps | 500 | no | Failed to inject causal disruption | no | `scenario_injection_errors_total` |

---

## Security contract

AUTHENTICATION: Evaluator authorization required for ground truth store.
AUTHORIZATION: Offline evaluation runner ONLY (`revpilot_evaluator`).
TENANT: Scenario targets `ten_alpha`; `ten_beta` is protected control.
PII: Prohibited.
SECRET ACCESS: Prohibited.
DEFAULT: DENY.

---

## Verification

COMMANDS:
```powershell
pytest tests/benchmark/test_scenario_injection.py tests/eval/test_ground_truth_airgap.py -v
```

EXPECTED: exit code 0 and all tests passing.

---

## Binary acceptance criteria

- `AC-P01-004-01`: PASS only if Midwest truck-capacity incident elevates affected cohort cancellation rate by +6.6% (+/- 0.5% tolerance).
- `AC-P01-004-02`: PASS only if `ten_beta` and unaffected `ten_alpha` facilities maintain baseline cancellation rates (no leakage across control cohorts).
- `AC-P01-004-03`: PASS only if runtime canonical entity schemas contain zero ground-truth label attributes.
- `AC-P01-004-04`: PASS only if unauthenticated or runtime access to `EvaluationGroundTruthStore` fails closed with `AccessDeniedError`.

---

## Readiness score

| Dimension | 0–2 | Rationale |
|---|---:|---|
| Objective clarity | 2 | Exact scenario mechanics, causal effects, and air-gap rules. |
| Dependency clarity | 2 | Strictly depends on `TASK-P01-003`. |
| File boundary clarity | 2 | Subpackages `scenarios/` and `ground_truth.py`. |
| Contract clarity | 2 | Typed ground-truth container and injector interface. |
| Failure behavior | 2 | Explicit security error on unauthorized access. |
| Security/tenancy | 2 | Air-gap access control and dual-tenant control cohort. |
| Testability | 2 | Statistical assertions on cancellation rate + air-gap tests. |
| Verification | 2 | Pytest commands specified. |
| Acceptance criteria | 2 | Four binary criteria. |
| Reasoning independence | 2 | Causal numbers and equations fully specified; 0 ambiguities. |
| **Total** | **20/20** | Implementation-ready. |

---

## Required executor report

Use the exact result contract from `execution/MICRO-TASK-RAIL-SYSTEM.md#executor-success-report`.
