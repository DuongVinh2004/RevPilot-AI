# TASK-P08-005 — Backup, Restore, and Disaster Recovery Validation

TYPE: MICRO-TASK
STATUS: DRAFT
RAIL: 5 — Persistence Isolation Foundation
INITIATIVE: REVPILOT
PHASE: PHASE-08
EPIC: EPIC-08-PRODUCTION-READINESS
FEATURE: FEAT-DISASTER-RECOVERY
COMPLEXITY: M
REASONING_LOAD: MEDIUM
READINESS_SCORE: 20/20
NEW_ARCHITECTURAL_DECISIONS_ALLOWED: 0
PARALLEL_SAFE: YES
PARALLEL_WITH: TASK-P08-001, TASK-P08-002, TASK-P08-003, TASK-P08-004
DEPENDS_ON: TASK-P07-008
UNLOCKS: TASK-P08-008

## Objective and rationale

OBJECTIVE: Implement the automated disaster recovery rehearsal runner (`revpilot.operations.dr.runner`) executing cold restores, continuous WAL replaying, cryptographic audit hash chain validation, and tenant-scoped extraction in an isolated staging environment.
BUSINESS RATIONALE: Demonstrating regulatory resilience and commercial business continuity mandates empirical proof that backups can be restored within target RPO ($\le 5$m) and RTO ($\le 30$m) without cross-tenant data corruption (`NFR-REC-001`, `INV-REL-001`, `AC-010`).
ARCHITECTURAL OWNER: SRE Lead & Storage Architect
ARCHITECTURAL CONTEXT: Persistence recovery and disaster response automation.

## Canonical specification references

- `docs/24-sre/DR-PLAN.md` §1..§4
- `docs/24-sre/BACKUP-RESTORE-VALIDATION-RUNBOOK.md` §1..§9
- `docs/29-testing/TEST-STRATEGY.md` §13 (`TC-P08-019`)

## Scope

- In-Scope: Implementing the automated restore validation harness, WAL replay verifier, and tenant extraction utility for staging environments.
- Non-Goals: Overwriting active production databases during rehearsals.

## Preconditions

- Phase 07 exit gate accepted (`TASK-P07-008`).
- Staging recovery PostgreSQL target available.

## Inputs

- Base backup snapshot fixtures and synthetic continuous WAL archive streams.

## Files to inspect

- `docs/24-sre/DR-PLAN.md`
- `docs/24-sre/BACKUP-RESTORE-VALIDATION-RUNBOOK.md`

## Files to create/update

CREATE:
- `packages/backend/src/revpilot/modules/operations/dr/runner.py`
- `packages/backend/src/revpilot/modules/operations/dr/wal_verifier.py`
- `packages/backend/src/revpilot/modules/operations/dr/tenant_extract.py`
- `tests/recovery/test_cold_restore_rehearsal.py`
- `tests/recovery/test_tenant_scoped_recovery.py`

DO NOT MODIFY:
- `packages/backend/src/revpilot/shared/*`
- `docs/*`

WRITE_SET:
- `packages/backend/src/revpilot/modules/operations/dr/runner.py`
- `packages/backend/src/revpilot/modules/operations/dr/wal_verifier.py`
- `packages/backend/src/revpilot/modules/operations/dr/tenant_extract.py`
- `tests/recovery/test_cold_restore_rehearsal.py`
- `tests/recovery/test_tenant_scoped_recovery.py`

## Detailed work items

1. Implement `DisasterRecoveryRunner` executing automated restore into an ephemeral database target.
2. Implement `WalVerifier` checking unbroken WAL sequence and calculating empirical RPO/RTO metrics.
3. Implement `TenantDataExtractor` performing isolated partition dump and restore for single-tenant recovery.
4. Author automated recovery tests verifying that restored data passes the complete cross-tenant isolation negative suite.

## Invariants

- `NFR-REC-001`: Target RPO $\le 5$m, RTO $\le 30$m verified during rehearsal.
- `INV-TEN-001`: Restored databases maintain 100% tenant RLS and storage isolation.

## Symbol-level contract

```python
class RecoveryRehearsalResult(BaseModel):
    snapshot_id: str
    target_time: UtcDateTime
    measured_rto_seconds: float
    measured_rpo_seconds: float
    audit_chain_verified: bool
    isolation_suite_passed: bool

class DisasterRecoveryRunner:
    def execute_cold_restore(self, snapshot_path: Path, wal_archive_path: Path, target_time: UtcDateTime) -> RecoveryRehearsalResult: ...
    def extract_single_tenant_slice(self, source_db_uri: str, tenant_id: TenantId) -> Path: ...
```

## Error contract

| Code | Trigger | Status | Retryable | Client Message | Audit |
|---|---|---:|---|---|---:|
| `RESTORE_TIMEOUT` | RTO exceeds 30 minutes | 500 | no | Recovery SLA exceeded | yes |
| `WAL_GAP_DETECTED` | Missing WAL archive block | 422 | no | Data loss detected in WAL stream | yes |
| `RESTORE_ISOLATION_BREACH`| Cross-tenant query leaks| 500 | no | Restored database corrupted | yes |

## Acceptance criteria

- `AC-P08-005-01`: PASS only if cold restore rehearsal executes in staging and achieves measured RTO $\le 30$m and RPO $\le 5$m.
- `AC-P08-005-02`: PASS only if restored database successfully passes 100% of the cross-tenant isolation negative matrix.

## Evidence required

- Passing test execution report `test_cold_restore_rehearsal.py` and signed recovery rehearsal manifest.

## Validation commands

```bash
pytest tests/recovery/test_cold_restore_rehearsal.py tests/recovery/test_tenant_scoped_recovery.py -v
```

## Dependencies and blocking

- DEPENDS_ON: `TASK-P07-008`
- BLOCKS: `TASK-P08-008`

## Risk controls

- Mitigates `RISK-018`: Ephemeral restore validation prevents silent backup degradation.

## Rollback and recovery

- Rehearsal targets ephemeral staging resources; failure tears down test container with zero production impact.

## Readiness score

| Dimension | Score (0–2) | Rationale |
|---|---:|---|
| Objective clarity | 2 | Disaster recovery runner and WAL verification specified. |
| Dependency clarity | 2 | Depends explicitly on TASK-P07-008. |
| File boundary clarity | 2 | Exact read/write/create paths provided. |
| Contract clarity | 2 | Clear DisasterRecoveryRunner and TenantExtractor interfaces. |
| Failure behavior | 2 | Complete error handling for WAL gaps and RTO exceedance. |
| Security/tenancy | 2 | Air-gapped staging execution; full isolation matrix verified. |
| Testability | 2 | Dedicated cold restore and tenant extraction test suites. |
| Verification | 2 | Non-destructive pytest command specified. |
| Acceptance criteria | 2 | Two machine-verifiable binary criteria defined. |
| Postconditions | 2 | Postconditions and exported symbols verified. |
| **Total** | **20/20** | Implementation ready. |
