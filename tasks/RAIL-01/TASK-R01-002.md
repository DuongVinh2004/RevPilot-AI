# TASK-R01-002 — Implement Error Hierarchy, Result Types, and Correlation Context

TYPE: MICRO-TASK  
STATUS: PASS  
RAIL: 1  
INITIATIVE: REVPILOT  
PHASE: PHASE-01  
EPIC: RAIL-01-CORE-PRIMITIVES  
FEATURE: SHARED-KERNEL-ERROR-RESULT-PRIMITIVES  
COMPLEXITY: S  
REASONING_LOAD: LOW  
READINESS_SCORE: 20/20  
NEW_ARCHITECTURAL_DECISIONS_ALLOWED: 0  
PARALLEL_SAFE: NO  
PARALLEL_WITH: NONE  
DEPENDS_ON: TASK-R01-001  
UNLOCKS: TASK-R01-003  

---

## Objective and rationale

OBJECTIVE: Implement standard domain error hierarchy, typed functional `Result[T, E]` monad, and structured `CorrelationContext` for request/event tracing across all bounded contexts.

BUSINESS RATIONALE: Enforce fail-closed error contracts, deterministic exception handling, and distributed observability correlation without leaking internal exceptions or customer secrets to API clients.

ARCHITECTURAL OWNER: Backend Architecture / Platform Core  
ARCHITECTURAL CONTEXT: Conforms to `ADR-0001`, `ADR-0010` (Observability), `docs/04-system-architecture/REPOSITORY-TOPOLOGY.md` §shared-kernel-limits, and `execution/IMPLEMENTATION-STANDARDS.md` §engineering-invariants.

---

## Canonical specification references

- `docs/04-system-architecture/REPOSITORY-TOPOLOGY.md` §shared-kernel-limits
- `docs/04-system-architecture/DEPENDENCY-RULES.md` §cross-module-rules
- `docs/03-requirements/INVARIANT-REGISTRY.md` (`INV-REL-001`, `INV-AUD-001`)
- `docs/31-adr/ADR-0010-observability-baseline.md`
- `execution/IMPLEMENTATION-STANDARDS.md` §engineering-invariants

---

## Preconditions

1. `TASK-R01-001` is in `PASS` status.
2. `packages/backend/src/revpilot/shared/` contains value primitives.
3. Automated test suite passes.

---

## Change boundary

### READ_SET

- `tasks/RAIL-01/TASK-R01-001.md`
- `docs/04-system-architecture/REPOSITORY-TOPOLOGY.md`
- `execution/IMPLEMENTATION-STANDARDS.md`

### WRITE_SET

- `packages/backend/src/revpilot/shared/errors.py`
- `packages/backend/src/revpilot/shared/results.py`
- `packages/backend/src/revpilot/shared/correlation.py`
- `packages/backend/tests/shared/test_errors_and_results.py`

### CREATE

All items in `WRITE_SET`.

### MODIFY

NONE.

### DO NOT MODIFY

- `apps/**`
- `docs/**`
- `infra/**`
- `config/**`

---

## Symbol-level contract

### `packages/backend/src/revpilot/shared/errors.py`

- MUST EXPORT:
  - `DomainError(code: str, message: str, details: dict, retryable: bool)`
  - `NotFoundError(message: str, details: dict)` (code: `NOT_FOUND`, retryable: False)
  - `ValidationError(message: str, details: dict)` (code: `VALIDATION_ERROR`, retryable: False)
  - `ConflictError(message: str, details: dict)` (code: `CONFLICT`, retryable: False)
  - `AuthorizationError(message: str, details: dict)` (code: `FORBIDDEN`, retryable: False)
  - `AuthenticationError(message: str, details: dict)` (code: `UNAUTHORIZED`, retryable: False)
  - `TenancyViolationError(message: str, details: dict)` (code: `TENANCY_VIOLATION`, retryable: False)
  - `ConcurrencyError(message: str, details: dict)` (code: `CONCURRENCY_CONFLICT`, retryable: True)
  - `RateLimitExceededError(message: str, details: dict)` (code: `RATE_LIMITED`, retryable: True)
  - `.to_dict() -> dict[str, Any]`

### `packages/backend/src/revpilot/shared/results.py`

- MUST EXPORT:
  - `Result[T, E]`
  - `Success[T](value: T)`
  - `Failure[E](error: E)`
  - Methods: `.is_success`, `.is_failure`, `.unwrap()`, `.unwrap_error()`, `.map()`, `.map_err()`, `.and_then()`

### `packages/backend/src/revpilot/shared/correlation.py`

- MUST EXPORT:
  - `CorrelationContext(trace_id: str, span_id: str, correlation_id: str, causation_id: str | None)`
  - `.create_root() -> CorrelationContext`
  - `.create_child(span_id: str | None) -> CorrelationContext`
  - `.to_dict() -> dict[str, str]`

---

## Verification commands

```powershell
python -m pytest packages/backend/tests/shared/test_errors_and_results.py -v
```

---

## Binary acceptance criteria

- `AC-R01-002-01`: PASS only if `DomainError` and all subclasses serialize to dict with stable codes and retryable flags.
- `AC-R01-002-02`: PASS only if `TenancyViolationError` fails closed with code `TENANCY_VIOLATION` and `retryable=False`.
- `AC-R01-002-03`: PASS only if `Result` implements functional `Success` and `Failure` branches with `.unwrap()`, `.map()`, and `.and_then()`.
- `AC-R01-002-04`: PASS only if `CorrelationContext` generates root context and properly derives child span/causation IDs.
- `AC-R01-002-05`: PASS only if `python -m pytest packages/backend/tests/shared/test_errors_and_results.py` passes 100% with exit code 0.

---

## Readiness score

| Dimension | Score (0–2) | Rationale |
|---|---:|---|
| Objective clarity | 2 | Concrete error, result, and correlation abstractions. |
| Dependency clarity | 2 | Depends on completed `TASK-R01-001`. |
| File boundary clarity | 2 | Exact 4 files in `WRITE_SET`. |
| Contract clarity | 2 | Explicit symbols, method signatures, and error codes. |
| Failure behavior | 2 | Fail-closed error handling and typed failure branches. |
| Security/tenancy | 2 | TenancyViolationError fail-closed; sanitized error output. |
| Testability | 2 | 100% unit testable with pytest. |
| Verification | 2 | Deterministic test command. |
| Acceptance criteria | 2 | 5 binary machine-verifiable criteria. |
| Reasoning independence | 2 | Zero architectural choices required. |
| **TOTAL** | **20/20** | **Ready for Queue Admission** |

---

## Definition of done

All acceptance criteria pass; verification exits 0; no unexpected/prohibited files changed; no TODO/FIXME/placeholder/skipped test/fake pass; executor result matches the mandatory report format.

---

## Execution result

- STATUS: PASS
- VERIFICATION: python -m pytest packages/backend/tests/shared/test_errors_and_results.py -v (9/9 PASSED, exit code 0)
- SCOPE_AUDIT: All 4 files in WRITE_SET created; 0 unauthorized, 0 prohibited, 0 secrets
- EXECUTOR: Gemini 3.8 Flash
- DATE: 2026-09-03

