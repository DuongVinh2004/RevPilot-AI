# TASK-R01-003 — Implement Tenant and Principal Context Primitives

TYPE: MICRO-TASK  
STATUS: PASS  
RAIL: 1  
INITIATIVE: REVPILOT  
PHASE: PHASE-01  
EPIC: RAIL-01-CORE-PRIMITIVES  
FEATURE: SHARED-KERNEL-CONTEXT-PRIMITIVES  
COMPLEXITY: S  
REASONING_LOAD: LOW  
READINESS_SCORE: 20/20  
NEW_ARCHITECTURAL_DECISIONS_ALLOWED: 0  
PARALLEL_SAFE: NO  
PARALLEL_WITH: NONE  
DEPENDS_ON: TASK-R01-002  
UNLOCKS: TASK-R01-004  

---

## Objective and rationale

OBJECTIVE: Implement trusted immutable `TenantContext`, `PrincipalContext`, and `SecurityContext` in the shared kernel to enforce server-side multi-tenancy and deny-by-default authorization boundaries.

BUSINESS RATIONALE: Enforce `INV-TEN-001`, `INV-TEN-002`, `INV-TEN-003`, and `INV-IAM-001` at compile/type level across all services, ensuring no business logic or database query can execute without explicit, validated tenant and principal context.

ARCHITECTURAL OWNER: Security Architecture / Tenancy Core  
ARCHITECTURAL CONTEXT: Conforms to `ADR-0005` (Tenant Isolation), `docs/04-system-architecture/REPOSITORY-TOPOLOGY.md` §shared-kernel-limits, and `docs/04-system-architecture/DEPENDENCY-RULES.md` §tenant-and-identity-propagation.

---

## Canonical specification references

- `docs/04-system-architecture/REPOSITORY-TOPOLOGY.md` §shared-kernel-limits
- `docs/04-system-architecture/DEPENDENCY-RULES.md` §tenant-and-identity-propagation
- `docs/03-requirements/INVARIANT-REGISTRY.md` (`INV-TEN-001`, `INV-TEN-002`, `INV-TEN-003`, `INV-IAM-001`)
- `docs/31-adr/ADR-0005-tenant-isolation.md`

---

## Preconditions

1. `TASK-R01-001` and `TASK-R01-002` are in `PASS` status.
2. `packages/backend/src/revpilot/shared/identifiers.py` exports `TenantId`, `PrincipalId`, `OrganizationId`.
3. Automated test suite passes.

---

## Change boundary

### READ_SET

- `packages/backend/src/revpilot/shared/identifiers.py`
- `packages/backend/src/revpilot/shared/errors.py`
- `docs/03-requirements/INVARIANT-REGISTRY.md`

### WRITE_SET

- `packages/backend/src/revpilot/shared/context.py`
- `packages/backend/tests/shared/test_context.py`

### CREATE

All items in `WRITE_SET`.

### MODIFY

NONE.

### DO NOT MODIFY

- `apps/**`
- `docs/**`
- `infra/**`
- `config/**`

---

## Symbol-level contract

### `packages/backend/src/revpilot/shared/context.py`

- MUST EXPORT:
  - `TenantContext(tenant_id: TenantId, organization_id: OrganizationId, tier: str, is_active: bool)`
  - `PrincipalContext(principal_id: PrincipalId, tenant_id: TenantId | None, roles: frozenset[str], permissions: frozenset[str], is_system: bool)`
  - `SecurityContext(tenant: TenantContext, principal: PrincipalContext)`
- Invariants:
  - Immutable (`slots=True`, `frozen=True`).
  - Rejects `tenant = None` unless explicitly a system principal (`INV-TEN-003`).
  - Rejects mismatched `tenant_id` between `PrincipalContext` and `TenantContext` in `SecurityContext`.

---

## Verification commands

```powershell
python -m pytest packages/backend/tests/shared/test_context.py -v
```

---

## Binary acceptance criteria

- `AC-R01-003-01`: PASS only if `TenantContext` strictly validates `TenantId` and `OrganizationId` and enforces immutability.
- `AC-R01-003-02`: PASS only if `PrincipalContext` rejects non-system principals with null tenant ID (`INV-TEN-003`).
- `AC-R01-003-03`: PASS only if `SecurityContext` raises `TenancyViolationError` when `principal.tenant_id != tenant.tenant_id`.
- `AC-R01-003-04`: PASS only if `python -m pytest packages/backend/tests/shared/test_context.py` passes 100% with exit code 0.

---

## Readiness score

| Dimension | Score (0–2) | Rationale |
|---|---:|---|
| Objective clarity | 2 | Concrete context value objects. |
| Dependency clarity | 2 | Depends on completed `TASK-R01-002`. |
| File boundary clarity | 2 | Exact 2 files in `WRITE_SET`. |
| Contract clarity | 2 | Strict invariant assertions specified. |
| Failure behavior | 2 | TenancyViolationError fail-closed. |
| Security/tenancy | 2 | Directly implements INV-TEN-001..003. |
| Testability | 2 | 100% unit-testable positive and negative matrix. |
| Verification | 2 | Deterministic pytest command. |
| Acceptance criteria | 2 | 4 binary machine-verifiable criteria. |
| Reasoning independence | 2 | Zero architectural choices required. |
| **TOTAL** | **20/20** | **Ready for Queue Admission** |

---

## Definition of done

All acceptance criteria pass; verification exits 0; no unexpected/prohibited files changed; no TODO/FIXME/placeholder/skipped test/fake pass; executor result matches the mandatory report format.

---

## Execution result

- STATUS: PASS
- VERIFICATION: python -m pytest packages/backend/tests/shared/test_context.py -v (9/9 PASSED, exit code 0)
- SCOPE_AUDIT: All 2 files in WRITE_SET created; 0 unauthorized, 0 prohibited, 0 secrets
- EXECUTOR: Gemini 3.8 Flash
- DATE: 2026-09-03

